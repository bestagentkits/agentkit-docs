---
title: ak insights evidence
description: "Xem bằng chứng chi tiết cho một lần thực thi."
---

Xem bằng chứng chi tiết cho một lần thực thi.

Dùng `ak insights evidence` với đối số và tùy chọn dưới đây. Phần help nguyên bản giữ nguyên thuật ngữ, cú pháp, tác động và điều kiện thực thi của CLI để đối chiếu chính xác.

## Contract và help của lệnh

Inspect an execution and its evaluation evidence

### Synopsis

Inspect an execution and its evaluation evidence. Unknown measurements remain unavailable; invocation success is not quality.

Effects:
  Read-only local evidence; no transcript content.

Exit status:
  0   success
  1   invalid input or unavailable local data
  2   invalid arguments

```
ak insights evidence <execution-id> [flags]
```

### Examples

```
  ak insights evidence execution-id --json
  ak insights evidence --help
```

### Options

```
  -h, --help             help for evidence
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak insights](./index)	 - Measure skill and agent effectiveness
