---
title: ak orchestrate prepare
description: "Chuẩn bị execution graph từ đầu vào kế hoạch."
---

Chuẩn bị execution graph từ đầu vào kế hoạch.

Dùng `ak orchestrate prepare` với đối số và tùy chọn dưới đây. Phần help nguyên bản giữ nguyên thuật ngữ, cú pháp, tác động và điều kiện thực thi của CLI để đối chiếu chính xác.

## Contract và help của lệnh

Validate and persist a resolved orchestration plan

### Synopsis

Validate and persist a resolved orchestration plan. CLI dispatch requires Darwin; internal dispatch belongs to the active harness.

Effects:
  Writes private jobs.yaml, state.json and metrics.jsonl in the run directory. Acceptance runs declared verification commands; advance may copy declared artifacts and spawn CLI jobs.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak orchestrate prepare <jobs.yaml> <run-dir> [flags]
```

### Examples

```
  ak orchestrate prepare jobs.yaml ./run
  ak orchestrate prepare jobs.yaml ./run --json
```

### Options

```
  -h, --help             help for prepare
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak orchestrate](./index)	 - Coordinate and inspect agent jobs (process supervision: Darwin)
