---
title: ak orchestrate diagnose
description: "Chẩn đoán trạng thái và vấn đề của tiến trình orchestration."
---

Chẩn đoán trạng thái và vấn đề của tiến trình orchestration.

Dùng `ak orchestrate diagnose` với đối số và tùy chọn dưới đây. Phần help nguyên bản giữ nguyên thuật ngữ, cú pháp, tác động và điều kiện thực thi của CLI để đối chiếu chính xác.

## Contract và help của lệnh

Export a bounded observation bundle for diagnosis

### Synopsis

Emit redacted observations and artifact/tool events for offline review. This replays evidence, not model execution, and never includes graph argv or environment.

Effects:
  Read-only. Redirect output to save a diagnostic bundle.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak orchestrate diagnose <run-id> [flags]
```

### Examples

```
  ak orchestrate diagnose <run-id> --json
  ak orchestrate diagnose <run-id> --json > diagnostic.json
```

### Options

```
  -h, --help             help for diagnose
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak orchestrate](./index)	 - Coordinate and inspect agent jobs (process supervision: Darwin)
