---
title: CLI commands
description: Full command and flag reference, generated from the released ak binary.
generated: true
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="info">
  This section is **generated automatically** from `ak` releases. It will be
  populated on the first release sync with one page per command, each carrying
  the exact syntax and flags of the released binary.
</Callout>

Until the first sync lands, use `ak help` to explore commands locally:

```bash
ak help
ak help <command>
ak <command> --help
```

The generated reference is kept in English only — command syntax is not
translated. Task-oriented guides remain available in every language.
