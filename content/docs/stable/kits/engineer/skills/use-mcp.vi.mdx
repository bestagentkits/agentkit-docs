---
title: Invocation đã retired ak:use-mcp
description: ak:use-mcp không còn được đóng gói như Skill riêng.
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="info" title="Invocation đã retired">
  `ak:use-mcp` không còn được đóng gói như Skill riêng. Tiếp tục với [`ak:mcp-builder`](./mcp-builder) để triển khai MCP server, [`ak:webmcp`](./webmcp) cho tool trên trang, hoặc [`ak:agent-browser`](./agent-browser) cho browser automation.
</Callout>

Đừng gọi `ak:use-mcp`. Route này giữ bookmark hiện có. Trang không mô tả
identity trong package hay lệnh có thể chạy.
