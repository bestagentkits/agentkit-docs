---
title: Invocation đã retired ak:security-scan map sang ak:security
description: ak:security-scan không còn được đóng gói như Skill riêng. Dùng ak:security.
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="info" title="Invocation đã retired">
  `ak:security-scan` không còn được đóng gói như Skill riêng. Tiếp tục với
  [`ak:security`](./security). `ak:security` mặc định là replacement comprehensive.
  `--secrets-only` và `--deps-only` bỏ code pattern và threat modeling; chúng
  không phải scan cũ gồm secret, dependency và code.
</Callout>

Đừng gọi `ak:security-scan`. Route này giữ bookmark hiện có. Trang không mô tả
identity trong package hay lệnh có thể chạy.
