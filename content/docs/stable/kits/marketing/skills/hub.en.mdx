---
title: Retired ak:hub
description: ak:hub is no longer packaged as a separate Skill.
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="info" title="Retired invocation">
  `ak:hub` is no longer packaged as a separate Skill. Continue with [`ak:content-hub`](./content-hub) when you need a content-hub workflow.
</Callout>

Do not invoke `ak:hub`. This route preserves existing bookmarks. It does not
describe a packaged identity or a runnable command.
