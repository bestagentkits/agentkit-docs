---
title: Retired ak:skill
description: ak:skill is no longer packaged as a separate Skill.
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="info" title="Retired invocation">
  `ak:skill` is no longer packaged as a separate Skill. This Kit no longer ships a generic `ak:skill` identity.
</Callout>

Do not invoke `ak:skill`. This route preserves existing bookmarks. It does not
describe a packaged identity or a runnable command.
