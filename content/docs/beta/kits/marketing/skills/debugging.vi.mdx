---
title: Invocation đã retired ak:debugging map sang ak:debug
description: ak:debugging không còn được đóng gói như Skill riêng. Dùng ak:debug.
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="info" title="Invocation đã retired">
  `ak:debugging` không còn được đóng gói như Skill riêng. Tiếp tục với `ak:debug`. Identity đó nằm trong package của Kit này; route này không phải guide của nó.
</Callout>

Đừng gọi `ak:debugging`. Route này giữ bookmark hiện có. Trang không mô tả
identity trong package hay lệnh có thể chạy.
