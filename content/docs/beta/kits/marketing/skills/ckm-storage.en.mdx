---
title: Retired ak:ckm-storage maps to ak:storage
description: ak:ckm-storage is no longer packaged as a separate Skill. Use ak:storage.
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="info" title="Retired invocation">
  `ak:ckm-storage` is no longer packaged as a separate Skill. Continue with [`ak:storage`](./storage).
</Callout>

Do not invoke `ak:ckm-storage`. This route preserves existing bookmarks. It does not
describe a packaged identity or a runnable command.
