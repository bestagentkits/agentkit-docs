---
title: Retired ak:use-mcp
description: ak:use-mcp is no longer packaged as a separate Skill.
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="info" title="Retired invocation">
  `ak:use-mcp` is no longer packaged as a separate Skill. Continue with [`ak:mcp-builder`](./mcp-builder) to implement an MCP server, [`ak:webmcp`](./webmcp) for in-browser page tools, or [`ak:agent-browser`](./agent-browser) for browser automation.
</Callout>

Do not invoke `ak:use-mcp`. This route preserves existing bookmarks. It does not
describe a packaged identity or a runnable command.
