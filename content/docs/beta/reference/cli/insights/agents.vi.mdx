---
title: ak insights agents
description: "Xem hiệu quả Agent từ bằng chứng thực thi cục bộ."
---

Xem hiệu quả Agent từ bằng chứng thực thi cục bộ.

Dùng `ak insights agents` với đối số và tùy chọn dưới đây. Phần help nguyên bản giữ nguyên thuật ngữ, cú pháp, tác động và điều kiện thực thi của CLI để đối chiếu chính xác.

## Contract và help của lệnh

Inspect agents effectiveness

### Synopsis

Inspect agents effectiveness. Unknown measurements remain unavailable; invocation success is not quality.

Effects:
  Read-only local measurements.

Exit status:
  0   success
  1   invalid input or unavailable local data
  2   invalid arguments

```
ak insights agents [flags]
```

### Examples

```
  ak insights agents --json
  ak insights agents --help
```

### Options

```
      --agent string        Filter agent definition ID
      --cohort string       Filter comparable task cohort
      --experiment string   Filter experiment ID
  -h, --help                help for agents
      --json                Emit machine-readable JSON (implies --no-interactive)
      --model string        Filter resolved model
      --no-interactive      Disable interactive prompts (CI-safe)
      --previous-period     Compare the bounded time range with the equally long preceding period
  -q, --quiet               Suppress non-error output on stderr
      --revision string     Filter authored artifact revision digest
      --rubric string       Select evaluation rubric (default "acceptance")
      --runtime string      Filter runtime
      --since int           Inclusive start time in Unix milliseconds
      --skill string        Filter canonical skill ID
      --task-type string    Filter task category
      --until int           Exclusive end time in Unix milliseconds
  -V, --verbose             Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes                 Assume yes for all prompts
```

### SEE ALSO

* [ak insights](./index)	 - Measure skill and agent effectiveness
