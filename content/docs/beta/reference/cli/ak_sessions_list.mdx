---
title: "ak sessions list"
description: "List Claude Code sessions for registered projects"
generated: true
---

List Claude Code sessions for registered AgentKit projects—a finite snapshot of session JSONL files.

**When to use it:** Before sharing, redacting, or inspecting local Claude Code session transcripts.

Read-only. Reads `~/.agentkit/projects.json` and `~/.claude/projects/*/*.jsonl`; `AGENTKIT_HOME` and `AGENTKIT_CLAUDE_HOME` override those roots.

### Usage

```bash
ak sessions list [flags]
```

### Examples

```bash
ak sessions list --json
ak sessions list --project myapp --json
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--project stringArray` | Project name to list; repeatable |

### Related commands

- [`ak sessions`](./ak_sessions) — Inspect and maintain local coding-agent sessions
