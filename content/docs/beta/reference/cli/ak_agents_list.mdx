---
title: "ak agents list"
description: "List installed agents"
generated: true
---

List the agents installed on this machine, optionally filtered by kit or tag.

**When to use it:** Run before `ak agents install` or `ak agents remove` to confirm what is and isn't already on disk.

This command is read-only.

### Usage

```bash
ak agents list [flags]
```

### Examples

```bash
ak agents list
ak agents list --kit engineer
ak agents list --json | jq .agents
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--kit string` | Filter by kit name |
| `--tag strings` | Filter by tag (repeatable) |

### Related commands

- [`ak agents`](./ak_agents) — Per-agent management commands
