---
title: "ak plan parse"
description: "Parse and display a structured plan view"
generated: true
---

Parse a plan directory and print a structured summary of phases, task counts, and progress.

**When to use it:** Inspecting plan structure, dumping a parsed view for tooling, or feeding into a script via `--json`.

Read-only.

### Usage

```bash
ak plan parse <path> [flags]
```

### Examples

```bash
ak plan parse ./plans/my-plan
ak plan parse ./plans/my-plan --json
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Exit codes

| Code | Meaning |
| --- | --- |
| `1` | plan directory not found or invalid format |

### Related commands

- [`ak plan`](./ak_plan) — Manage AgentKit plan directories
