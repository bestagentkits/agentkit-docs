---
title: "ak config status"
description: "Show the running state of the dashboard server"
generated: true
---

Report whether the dashboard server is running, showing PID, bind address, and uptime when active. Exits 0 whether or not the server is running—use the `running` field in `--json` output to branch in scripts.

**When to use it:** Check whether `ak config start` has been run, or verify the dashboard is still up after a long idle period. Safe to call repeatedly; read-only.

Reads `~/.agentkit/dashboard-state.json`.

### Usage

```bash
ak config status [flags]
```

### Examples

```bash
ak config status          # human-readable one-liner
ak config status --json   # JSON envelope with schema_version, running, url, pid
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak config`](./ak_config) — Open the local AgentKit dashboard
