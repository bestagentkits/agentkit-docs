---
title: "ak commands search"
description: "Search commands"
generated: true
---

Search available commands by name, description, and tags. Results are ranked deterministically; ties broken by &lt;kit>/&lt;command> alphabetical order.

**Who it's for:** Power devs scripting (--json) and users discovering what's available.

**When to use it:** To find a command by keyword without knowing the exact name.

**What changes on disk:** Read-only.

### Usage

```bash
ak commands search <query> [flags]
```

### Examples

```bash
ak commands search browser
ak commands search 'code review' --json
```

### Flags

| Flag | Description |
| --- | --- |
| `-h, --help` | help for search |
| `--json` | Emit machine-readable JSON (implies --no-interactive) |
| `--no-interactive` | Disable interactive prompts (CI-safe) |
| `-q, --quiet` | Suppress non-error output on stderr |
| `-V, --verbose` | Extra diagnostic output on stderr (loses to --quiet) |
| `-y, --yes` | Assume yes for all prompts |

### Output modes

| Mode | Behavior |
| --- | --- |
| `pretty` | default on TTY (colors, ASCII markers) |
| `plain` | auto when stdout piped or --no-interactive |
| `json` | --json (single-object envelope, NDJSON-safe) |

### Exit codes

| Code | Meaning |
| --- | --- |
| `0` | success |
| `1` | runtime error |
| `2` | invalid flags |
| `3` | user-cancel (SIGINT, prompt-cancel) |
| `2` | empty query |

### Related commands

- [`ak commands`](./ak_commands) — Per-slash-command management commands
