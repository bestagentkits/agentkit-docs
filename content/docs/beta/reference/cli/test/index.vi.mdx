---
title: ak test
description: "Lập kế hoạch và chạy các check test-impact của repository."
---

Lập kế hoạch và chạy các check test-impact của repository.

Dùng `ak test` với đối số và tùy chọn dưới đây. Phần help nguyên bản giữ nguyên thuật ngữ, cú pháp, tác động và điều kiện thực thi của CLI để đối chiếu chính xác.

## Contract và help của lệnh

Plan and execute repository test-impact checks

### Synopsis

Use repository-owned manifests to select checks conservatively. Mandatory policy checks always apply. Semantic observations require explicit consumer consent; default planning stays offline.

### Examples

```
  ak test optimize --base origin/dev --json
  ak test benchmark corpus.json --json
```

### Options

```
  -h, --help             help for test
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak](./ak)	 - AgentKit CLI
* [ak test benchmark](./benchmark)	 - Execute trusted regression corpus in isolated temporary workspaces
* [ak test classify-e2e](./classify-e2e)	 - Advise on changed E2E scenario classes without changing scheduling
* [ak test optimize](./optimize)	 - Explain impact-selected checks, optionally execute trusted commands
