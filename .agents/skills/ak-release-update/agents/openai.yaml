interface:
  display_name: "AgentKit Docs Release Update Orchestrator"
  short_description: "End-to-end docs release update pipeline"
  default_prompt: "Use $ak-release-update to orchestrate a docs release update — resolve evidence, delegate audit, run scripts, and open the PR."
