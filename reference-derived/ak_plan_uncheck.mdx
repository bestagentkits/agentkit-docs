---
title: "ak plan uncheck"
description: "Mark all checkboxes in a phase file as todo"
generated: true
---

Mark all checkboxes in a phase file as todo by toggling `- [x]` back to `- [ ]`. Already-unchecked boxes are left unchanged—idempotent.

**When to use it:** When you need to reopen a completed phase for further work.

Mutates the phase file in-place with an atomic write.

### Usage

```bash
ak plan uncheck <phase-file> [flags]
```

### Examples

```bash
ak plan uncheck ./plans/my-plan/phase-01-setup.md
ak plan uncheck ./plans/my-plan/phase-01-setup.md --json
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Exit codes

| Code | Meaning |
| --- | --- |
| `1` | phase file not found or I/O error |

### Related commands

- [`ak plan`](./ak_plan) — Manage AgentKit plan directories
