---
title: "ak plan phase close"
description: "Mark a phase as done"
generated: true
---

Complete phase &lt;n&gt; of plan &lt;plan-id&gt; in its canonical phase file — the files-first path `ak plan check` takes: a phase with checkboxes has them all marked done, a checkbox-less phase gets its front-matter status set to done. The plan store is then re-synced from the file, so the change survives `ak plan reindex`.

**Effects:** Mutates the phase file in-place (atomic write), then re-syncs the local plan store from it.

**Exit status:** 0 success 1 plan/phase not found, no linked plan directory, or file / plan store I/O error 2 invalid arguments

### Usage

```bash
ak plan phase close <plan-id> <n> [flags]
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak plan phase`](./ak_plan_phase) — Manage phases of a plan tracked in the local plan store
