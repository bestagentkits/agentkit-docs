---
title: "ak insights record"
description: "Import versioned execution observations"
generated: true
---

Import versioned execution observations. Unknown measurements remain unavailable; invocation success is not quality.

**Effects:** Writes validated local measurements when collection is enabled.

### Usage

```bash
ak insights record [flags]
```

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--file string` | JSON batch file |

### Exit codes

| Code | Meaning |
| --- | --- |
| `1` | invalid input or unavailable local data |
| `2` | invalid arguments |

### Related commands

- [`ak insights`](./ak_insights) — Measure skill and agent effectiveness
