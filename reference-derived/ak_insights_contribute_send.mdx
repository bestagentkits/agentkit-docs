---
title: "ak insights contribute send"
description: "Send a previously previewed aggregate"
generated: true
---

Send a previously previewed aggregate. Unknown measurements remain unavailable; invocation success is not quality.

**Effects:** Transmits only the frozen aggregate to the configured approved HTTPS endpoint when sharing consent is enabled.

### Usage

```bash
ak insights contribute send [flags]
```

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--digest string` | SHA-256 from the exact preview |
| `--endpoint string` | Approved HTTPS endpoint; defaults to AGENTKIT_CONTRIBUTION_ENDPOINT |
| `--month string` | Previously previewed UTC month |

### Exit codes

| Code | Meaning |
| --- | --- |
| `1` | invalid input or unavailable local data |
| `2` | invalid arguments |

### Related commands

- [`ak insights contribute`](./ak_insights_contribute) — Preview and voluntarily share aggregate measurements
