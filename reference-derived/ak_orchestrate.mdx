---
title: "ak orchestrate"
description: "Coordinate and inspect agent jobs (process supervision: Darwin)"
generated: true
---

Discover runtimes, coordinate artifact-backed jobs, and inspect durable events. External process supervision requires Darwin; historical observation and runtime discovery do not claim process-control support.

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak`](./ak) — AgentKit CLI
- [`ak orchestrate accept`](./ak_orchestrate_accept) — Verify and accept one settled attempt's artifact receipt
- [`ak orchestrate advance`](./ak_orchestrate_advance) — Reconcile attempts and dispatch dependency-ready jobs
- [`ak orchestrate diagnose`](./ak_orchestrate_diagnose) — Export a bounded observation bundle for diagnosis
- [`ak orchestrate events`](./ak_orchestrate_events) — Read a bounded page of durable run events
- [`ak orchestrate output`](./ak_orchestrate_output) — Read bounded redacted job output by byte offset
- [`ak orchestrate plan-status`](./ak_orchestrate_plan-status) — Inspect persisted plan state and invalidate stale evidence
- [`ak orchestrate prepare`](./ak_orchestrate_prepare) — Validate and persist a resolved orchestration plan
- [`ak orchestrate probe`](./ak_orchestrate_probe) — Discover installed coding runtimes without inference or authentication
- [`ak orchestrate resume`](./ak_orchestrate_resume) — Reconnect to an existing run after a client or coordinator crash
- [`ak orchestrate start`](./ak_orchestrate_start) — Launch a new orchestrated run from a job graph file
- [`ak orchestrate status`](./ak_orchestrate_status) — Report a run's current lifecycle state
- [`ak orchestrate stop`](./ak_orchestrate_stop) — Terminate a run's live jobs (TERM, grace period, then KILL)
