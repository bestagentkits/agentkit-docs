---
title: "ak insights evaluate"
description: "Record an outcome assessment with evidence"
generated: true
---

Record an outcome assessment with evidence. Unknown measurements remain unavailable; invocation success is not quality.

**Effects:** Appends a local evaluator verdict; never overwrites earlier evidence.

### Usage

```bash
ak insights evaluate [flags]
```

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--file string` | Evaluation JSON file |

### Exit codes

| Code | Meaning |
| --- | --- |
| `1` | invalid input or unavailable local data |
| `2` | invalid arguments |

### Related commands

- [`ak insights`](./ak_insights) — Measure skill and agent effectiveness
