---
title: "ak plan archive"
description: "Archive a plan in the local plan store"
generated: true
---

Mark a plan as archived in the local plan store. A VACUUM INTO recovery snapshot is taken before the state change. Archived plans are excluded from `ak plan list` by default.

**Who it's for:** Both: long-term retention of finished work and scripted automation.

**When to use it:** (unspecified)

**What changes on disk:** Writes the plans table and a recovery snapshot in the local plan store.

### Usage

```bash
ak plan archive <id> [flags]
```

### Examples

```bash
ak plan archive my-project/260722-1200
ak plan archive my-project/260722-1200 --json
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Exit codes

| Code | Meaning |
| --- | --- |
| `1` | plan not found or plan store I/O error |

### Related commands

- [`ak plan`](./ak_plan) — Manage AgentKit plan directories
