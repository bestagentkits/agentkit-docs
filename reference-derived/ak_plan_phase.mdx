---
title: "ak plan phase"
description: "Manage phases of a plan tracked in the local plan store"
generated: true
---

Namespace for phase-level mutations on a plan tracked in the local SQLite plan store: `phase update` upserts a phase's fields, `phase close` marks a phase done.

**Who it's for:** Both: manual phase bookkeeping and scripted automation.

**When to use it:** (unspecified)

**What changes on disk:** See subcommands.

### Examples

```bash
ak plan phase update my-project/260722-1200 1 --status in-progress
ak plan phase close my-project/260722-1200 1
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak plan`](./ak_plan) — Manage AgentKit plan directories
- [`ak plan phase close`](./ak_plan_phase_close) — Mark a phase as done
- [`ak plan phase update`](./ak_plan_phase_update) — Create or update a phase record for a plan
