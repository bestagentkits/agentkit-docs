---
title: "ak eval triage-benchmark"
description: "Evaluate frozen finding triage against local verified labels"
generated: true
---

Evaluate frozen finding triage against local verified labels. Input must contain compact reviewed facts; raw scanner output is not accepted.

**Effects:** Reads local frozen replay and labels only; never calls a provider or runs review commands.

### Usage

```bash
ak eval triage-benchmark <input.json|-> [flags]
```

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Exit codes

| Code | Meaning |
| --- | --- |
| `0` | report generated; inspect safety and coverage |
| `1` | invalid input or configuration |

### Related commands

- [`ak eval`](./ak_eval) — Run controlled skill and agent evaluations
