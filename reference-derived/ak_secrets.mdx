---
title: "ak secrets"
description: "Manage named secret values (API keys, tokens)"
generated: true
---

Store, read, list, delete, and rotate small named secret values behind the OS keychain, an age-encrypted vault (--backend=file, for a host with no usable keychain; see `ak secrets recovery`), or an in-memory --backend=memory dev backend. Values are written only to the active backend; ~/.agentkit/secrets/index.json holds metadata (ref, creator, timestamps) and never a value. Use it for an API key, token, or certificate AgentKit or an adapter needs at runtime, instead of an unmanaged .env file or shell export.

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak`](./ak) — AgentKit CLI
- [`ak secrets approve`](./ak_secrets_approve) — Answer the secret requests agents left waiting
- [`ak secrets audit`](./ak_secrets_audit) — Show the secrets access ledger and matching grants
- [`ak secrets delete`](./ak_secrets_delete) — Delete a stored secret
- [`ak secrets export`](./ak_secrets_export) — Emit a versioned inventory of every stored secret
- [`ak secrets get`](./ak_secrets_get) — Show a secret's metadata, redacted by default
- [`ak secrets grant`](./ak_secrets_grant) — Let a fingerprinted agent binary read a secret
- [`ak secrets grants`](./ak_secrets_grants) — List recorded agent grants
- [`ak secrets import-env`](./ak_secrets_import-env) — Bulk-import secrets from a dotenv file
- [`ak secrets list`](./ak_secrets_list) — List stored secret refs and metadata
- [`ak secrets recovery`](./ak_secrets_recovery) — Manage the file backend's vault and its recovery keys
- [`ak secrets revoke`](./ak_secrets_revoke) — Remove an agent's grants on a secret, or all of them
- [`ak secrets rotate`](./ak_secrets_rotate) — Replace an existing secret's value
- [`ak secrets set`](./ak_secrets_set) — Store a secret value
- [`ak secrets trust`](./ak_secrets_trust) — Show or re-pin a coding agent's trusted binary
