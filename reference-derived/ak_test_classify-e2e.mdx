---
title: "ak test classify-e2e"
description: "Advise on changed E2E scenario classes without changing scheduling"
generated: true
---

Compare top-level E2E tests against a Git base, including dirty and untracked files. Optional provider advice requires explicit e2e-classification consent and a key. Unknown effects cannot establish parallel hermetic isolation. The class manifest remains authoritative.

**Effects:** Reads local Git and Go source. When explicitly configured, sends only bounded structural facts to the provider. Optionally creates a receipt; never changes classifications or scheduling.

### Usage

```bash
ak test classify-e2e [flags]
```

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--base string` | Git base compared with the current checkout (required) |
| `--facts string` | Optional repository-local reviewed structural facts JSON |
| `--output string` | Create a new JSON advice receipt; never overwrite |

### Exit codes

| Code | Meaning |
| --- | --- |
| `0` | discovery and advisory status reported |
| `1` | invalid input, stale advice, or output failure |
| `2` | invalid arguments |

### Related commands

- [`ak test`](./ak_test) — Plan and execute repository test-impact checks
