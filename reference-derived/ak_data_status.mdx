---
title: "ak data status"
description: "Show default retention posture for derived classes"
generated: true
---

Lists known derived data classes and their default forever retention posture.

**Who it's for:** Operators auditing local data growth policy.

**When to use it:** During capacity planning or before changing retention.

**What changes on disk:** Read-only.

### Usage

```bash
ak data status [flags]
```

### Examples

```bash
ak data status --json
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak data`](./ak_data) — Inspect derived-data retention and run bounded ingest
