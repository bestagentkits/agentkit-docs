---
title: "ak watch status"
description: "Show current watch state (all repos, or a specific one)"
generated: true
---

Show current watch state for all repos or a specific one, including responded counts and daemon status.

**When to use it:** After `ak watch start --daemon` to confirm the watcher is running, or to inspect responded counts.

Read-only. Reads `~/.agentkit/watch/<repo-slug>/state.json`; `AGENTKIT_HOME` overrides the base directory.

### Usage

```bash
ak watch status [owner/repo] [flags]
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak watch`](./ak_watch) — Watch a GitHub repository and auto-respond to issues
