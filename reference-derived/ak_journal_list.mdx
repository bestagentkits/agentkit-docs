---
title: "ak journal list"
description: "List journal entries (newest first)"
generated: true
---

List journal summaries newest-first with optional date/query filters.

**Who it's for:** Agents and developers discovering prior work records.

**When to use it:** Browsing project history before authoring a new entry.

**What changes on disk:** Read-only.

### Usage

```bash
ak journal list [flags]
```

### Examples

```bash
ak journal list
ak journal list --query install --json
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--from string` | Include entries on/after YYYY-MM-DD |
| `--project string` | Limit to one registered project |
| `--query string` | Filter title/summary/slug text |
| `--to string` | Include entries on/before YYYY-MM-DD |

### Related commands

- [`ak journal`](./ak_journal) — Manage journals in configured plan storage
