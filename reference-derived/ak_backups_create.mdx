---
title: "ak backups create"
description: "Create a consistent operational.db snapshot via VACUUM INTO"
generated: true
---

Produces a database-aware snapshot of the durable operational core using SQLite VACUUM INTO.

**Effects:** Writes a verified operational snapshot under staging directory.

**Exit status:** 0 success, 1 command failure, 2 invalid flags or arguments

### Usage

```bash
ak backups create [flags]
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--include-content` | include rebuildable content shards (not default) |

### Related commands

- [`ak backups`](./ak_backups) — Manage rollback snapshots
