---
title: "ak eval ui"
description: "Report opt-in shadow UI regression signals"
generated: true
---

Reads a reviewed compact DOM/a11y/computed/journey capture. Default off makes no provider call. Shadow requires user provider consent, explicit ui-regression consumer grant, and key. Browser failures remain failures; unknown provider results retain the current workflow.

**Effects:** Optional paid text-only inference; no browser or vision calls. Creates a receipt only with --output. Never uploads image/video or changes test results.

### Usage

```bash
ak eval ui <state.json|-> [flags]
```

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--mode string` | off or shadow; never changes browser authority (default "off") |
| `--output string` | Create a new receipt without overwriting |

### Exit codes

| Code | Meaning |
| --- | --- |
| `0` | report available; inspect deterministic failures and unknown signals |
| `1` | invalid input, configuration or output failure |

### Related commands

- [`ak eval`](./ak_eval) — Run controlled skill and agent evaluations
