---
title: "ak journal"
description: "Manage journals in configured plan storage"
generated: true
---

Create, list, show, and validate chronological work journals in configured plan storage.

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak`](./ak) — AgentKit CLI
- [`ak journal create`](./ak_journal_create) — Create a journal in configured plan storage
- [`ak journal list`](./ak_journal_list) — List journal entries (newest first)
- [`ak journal show`](./ak_journal_show) — Show one journal entry
- [`ak journal validate`](./ak_journal_validate) — Validate journal frontmatter/title/date
