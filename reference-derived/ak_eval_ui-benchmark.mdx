---
title: "ak eval ui-benchmark"
description: "Measure independently labeled UI signals offline"
generated: true
---

Replays bound typed responses against independent assertion labels. Synthetic fixtures validate integration, not production quality or savings. Actual provider, reasoning and vision calls are zero; recorded measurements require provenance.

**Effects:** Reads local records; no network calls or writes.

### Usage

```bash
ak eval ui-benchmark <corpus.json|-> [flags]
```

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Exit codes

| Code | Meaning |
| --- | --- |
| `0` | valid benchmark; inspect useful, missed, and unknown counts |
| `1` | invalid corpus or replay |

### Related commands

- [`ak eval`](./ak_eval) — Run controlled skill and agent evaluations
