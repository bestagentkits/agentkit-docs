---
title: "ak policy adopt"
description: "Promote proposed rules into the active policy"
generated: true
---

Copy named proposals from .agentkit/policy-candidates.yaml into .agentkit/policy.yaml, where the engine reads them. This is the explicit confirmation a generated rule needs, which is why naming nothing is an error rather than adopting everything. A proposal whose id already names an active rule is refused rather than overwriting a decision someone made by hand, and the adopted rule records the candidate and the instruction it came from.

**Effects:** Rewrites .agentkit/policy.yaml in canonical form: no existing rule is changed or removed, but the file is re-rendered from its rules, so ordering is normalised and comments are not kept.

### Usage

```bash
ak policy adopt <candidate-id>... [flags]
```

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--all` | Adopt every proposal instead of naming them |
| `--dir string` | Project directory holding .agentkit/policy.yaml (default ".") |

### Exit codes

| Code | Meaning |
| --- | --- |
| `1` | no proposals, an unknown id, or a proposal that would overwrite an active rule |
| `2` | invalid arguments |
| `4` | the resulting document would conflict with itself, so nothing was written |

### Related commands

- [`ak policy`](./ak_policy) — Inspect and apply project agent policies
