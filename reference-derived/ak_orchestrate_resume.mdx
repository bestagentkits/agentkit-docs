---
title: "ak orchestrate resume"
description: "Reconnect to an existing run after a client or coordinator crash"
generated: true
---

Reconnect to run-id using the same job graph file. Never relaunches jobs; if the worker crashed, reclassifies persisted state only.

**Effects:** Read-only against a live worker; persists a reclassification of orphaned jobs when the worker crashed.

**Exit status:** 0 success 1 graph digest mismatch, run not found, or state is corrupt 2 invalid flags or arguments 6 unsupported platform (v1 requires darwin)

### Usage

```bash
ak orchestrate resume <run-id> <job-graph.json> [flags]
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak orchestrate`](./ak_orchestrate) — Run and supervise external CLI job graphs (Darwin only)
