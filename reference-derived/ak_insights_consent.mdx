---
title: "ak insights consent"
description: "Inspect or change local measurement consent"
generated: true
---

Inspect or change local measurement consent. Unknown measurements remain unavailable; invocation success is not quality.

**Effects:** Writes consent only for explicitly supplied flags. Sharing does not transmit data.

### Usage

```bash
ak insights consent [flags]
```

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--collection` | Enable or disable local collection |
| `--sharing` | Consent to voluntary allowlisted aggregate sharing; no upload is performed |

### Exit codes

| Code | Meaning |
| --- | --- |
| `1` | invalid input or unavailable local data |
| `2` | invalid arguments |

### Related commands

- [`ak insights`](./ak_insights) — Measure skill and agent effectiveness
