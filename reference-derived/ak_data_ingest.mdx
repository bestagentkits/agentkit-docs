---
title: "ak data ingest"
description: "Run one bounded Claude/Codex operational ingest sweep"
generated: true
---

Opens operational.db and runs one Engine.Run across Claude and Codex adapters. Runtime files remain session authority; this only refreshes the local read model.

**Who it's for:** Operators refreshing derived session metadata after runtime activity.

**When to use it:** After new Claude/Codex sessions exist, or when operationalread looks stale.

**What changes on disk:** Writes operational.db under AGENTKIT_HOME; never mutates runtime sources.

### Usage

```bash
ak data ingest [flags]
```

### Examples

```bash
ak data ingest --json
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak data`](./ak_data) — Inspect derived-data retention and run bounded ingest
