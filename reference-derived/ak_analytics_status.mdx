---
title: "ak analytics status"
description: "Show the safe local analytics lifecycle status"
generated: true
---

Shows whether local analytics is serving a verified disposable index or the authoritative live collector.

**Who it's for:** Both: operators and scripts that need a privacy-safe health signal.

**When to use it:** Before an explicit refresh, rebuild, disable, or delete operation.

**What changes on disk:** Read-only. Does not create an analytics directory, database, marker, lock, or recovery snapshot.

### Usage

```bash
ak analytics status [flags]
```

### Examples

```bash
ak analytics status
ak analytics status --json --no-interactive
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak analytics`](./ak_analytics) — Control the private local analytics index
