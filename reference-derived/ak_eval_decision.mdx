---
title: "ak eval decision"
description: "Compare an explicitly enabled semantic judgment with a legacy decision"
generated: true
---

**Run a shadow profile:** domain-routing, fix-complexity, or orchestration-risk. Input contains consumer, compact state and legacy answers. User-scope semantic_decision consent and a credential are required for external evaluation. Invoking this command does not enable the provider.

**Effects:** When explicitly configured, sends bounded redacted facts to the provider and may incur charges. Prints local comparison only; never changes the legacy decision or persists input.

### Usage

```bash
ak eval decision <request.json|-> [flags]
```

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Exit codes

| Code | Meaning |
| --- | --- |
| `0` | completed or disabled; inspect status |
| `1` | invalid input or enabled provider unavailable |

### Related commands

- [`ak eval`](./ak_eval) — Run controlled skill and agent evaluations
