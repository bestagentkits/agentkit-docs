---
title: "ak analytics rebuild"
description: "Rebuild the local analytics index"
generated: true
---

Previews the local analytics rebuild operation and applies it only after explicit confirmation.

**Who it's for:** Both: maintainers at a terminal and scripted maintenance jobs.

**When to use it:** When the private local analytics lifecycle needs an explicit state transition.

**What changes on disk:** Preview is read-only. Applying may update the disposable local analytics index; delete retains only a disabled intent marker.

### Usage

```bash
ak analytics rebuild [flags]
```

### Examples

```bash
ak analytics rebuild
ak analytics rebuild --yes --json --no-interactive
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak analytics`](./ak_analytics) — Control the private local analytics index
