---
title: "ak analytics rebuild"
description: "Rebuild the local analytics index"
generated: true
---

Fully rebuilds the private local SQLite analytics index from raw session and activity sources. Interactive terminals prompt before applying; pass --yes to bypass prompts.

**Effects:** Re-indexes ~/.agentkit/analytics/ SQLite database from scratch.

**Exit status:** 0 success 1 command failure 2 invalid flags or arguments 3 preview complete (re-run with --yes to apply)

### Usage

```bash
ak analytics rebuild [flags]
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak analytics`](./ak_analytics) — Control the private local analytics index
