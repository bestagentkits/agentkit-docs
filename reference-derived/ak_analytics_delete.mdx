---
title: "ak analytics delete"
description: "Delete the local analytics index"
generated: true
---

Deletes the disposable local SQLite analytics index database and retains only a disabled intent marker. Interactive terminals prompt before applying; pass --yes to bypass prompts.

**Effects:** Deletes ~/.agentkit/analytics/ SQLite database files.

**Exit status:** 0 success 1 command failure 2 invalid flags or arguments 3 preview complete (re-run with --yes to apply)

### Usage

```bash
ak analytics delete [flags]
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak analytics`](./ak_analytics) — Control the private local analytics index
