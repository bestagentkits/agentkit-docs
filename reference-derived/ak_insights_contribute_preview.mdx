---
title: "ak insights contribute preview"
description: "Inspect the exact voluntary aggregate payload"
generated: true
---

Inspect the exact voluntary aggregate payload. Unknown measurements remain unavailable; invocation success is not quality.

**Effects:** Freezes one closed-month aggregate locally; sends nothing.

### Usage

```bash
ak insights contribute preview [flags]
```

Global flags, output modes, and the base exit codes (`0`–`2`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--month string` | Closed UTC month, YYYY-MM |

### Exit codes

| Code | Meaning |
| --- | --- |
| `1` | invalid input or unavailable local data |
| `2` | invalid arguments |

### Related commands

- [`ak insights contribute`](./ak_insights_contribute) — Preview and voluntarily share aggregate measurements
