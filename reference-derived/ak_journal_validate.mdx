---
title: "ak journal validate"
description: "Validate journal frontmatter/title/date"
generated: true
---

Validate title/date presence for a journal path or id.

**Who it's for:** Agents and CI checking journal quality.

**When to use it:** After create or when auditing legacy entries.

**What changes on disk:** Read-only.

### Usage

```bash
ak journal validate [path-or-id] [flags]
```

### Examples

```bash
ak journal validate 2026-07-27-fix-install-race
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--project string` | Registered project name |

### Related commands

- [`ak journal`](./ak_journal) — Manage journals in configured plan storage
