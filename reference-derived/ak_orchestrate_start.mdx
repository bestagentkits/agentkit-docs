---
title: "ak orchestrate start"
description: "Launch a new orchestrated run from a job graph file"
generated: true
---

Validate a job graph file, generate a fresh run ID, and spawn a detached worker. Always creates a new run.

**Effects:** Writes AGENTKIT_HOME/orchestrate/runs/&lt;run-id&gt;/ (state, per-job logs, launch graph). Never persists prompts, argv, env, or secrets.

**Exit status:** 0 success 1 job graph invalid, or the worker failed to reach readiness 2 invalid flags or arguments 6 unsupported platform (v1 requires darwin)

### Usage

```bash
ak orchestrate start <job-graph.json> [flags]
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak orchestrate`](./ak_orchestrate) — Run and supervise external CLI job graphs (Darwin only)
