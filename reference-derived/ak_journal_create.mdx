---
title: "ak journal create"
description: "Create a journal entry under plans/journals"
generated: true
---

Create a collision-safe markdown journal under &lt;project&gt;/plans/journals/.

**Who it's for:** Agents and developers recording chronological work history.

**When to use it:** After meaningful implementation, reviews, or incident work.

**What changes on disk:** Writes one new .md file under plans/journals/. Never overwrites existing files.

### Usage

```bash
ak journal create <title> [flags]
```

### Examples

```bash
ak journal create "Fix install race" --summary "Root cause and fix"
printf 'body' | ak journal create "Notes" --stdin --date 2026-07-27
```

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Flags

| Flag | Description |
| --- | --- |
| `--date string` | Entry date YYYY-MM-DD (default: today UTC) |
| `--project string` | Registered project name (default: cwd match) |
| `--stdin` | Read journal body from stdin |
| `--summary string` | Short summary stored in frontmatter |

### Related commands

- [`ak journal`](./ak_journal) — Manage technical journal entries under plans/journals
