---
title: "ak analytics"
description: "Control the private local analytics index"
generated: true
---

Control AgentKit's private local SQLite analytics index. Session and activity sources remain authoritative; no remote analytics are sent.

Global flags, output modes, and the standard exit codes (`0`–`3`) are shared by every command — see [CLI conventions](../cli-conventions). The sections below list only what is specific to this command.

### Related commands

- [`ak`](./ak) — AgentKit CLI
- [`ak analytics delete`](./ak_analytics_delete) — Delete the local analytics index
- [`ak analytics disable`](./ak_analytics_disable) — Disable indexed reads without deleting the index
- [`ak analytics enable`](./ak_analytics_enable) — Enable the local analytics index
- [`ak analytics rebuild`](./ak_analytics_rebuild) — Rebuild the local analytics index
- [`ak analytics refresh`](./ak_analytics_refresh) — Refresh the local analytics index
- [`ak analytics status`](./ak_analytics_status) — Show the safe local analytics lifecycle status
