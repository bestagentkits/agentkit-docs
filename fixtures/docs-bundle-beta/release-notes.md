# v0.42.0-beta.7

Automated beta release cut after a green build.

## Changes

- `ak kit init` gained a `--channel` flag to select the release channel.
- `ak self-update` now prints verified changelog entries with `--changelog`.
- Fixed a race in the Windows self-update helper.
