# Cloudflare Workers static-assets redirects (_redirects). Both locales carry
# an explicit URL prefix (hideLocale: 'never'), so the bare root has no page —
# send it to the default locale. Static-export friendly: no middleware.
/            /en/         302
/docs        /en/docs     302
/docs/*      /en/docs/:splat  302
