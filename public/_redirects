# Cloudflare Pages redirects. Both locales carry an explicit URL prefix
# (hideLocale: 'never'), so the bare root has no page — send it to the default
# locale. Static-export friendly: no middleware, no server runtime.
/            /en/         302
/docs        /en/docs     302
/docs/*      /en/docs/:splat  302
