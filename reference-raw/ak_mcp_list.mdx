---
title: "ak mcp list"
description: "List discovered MCP servers"
generated: true
---

## ak mcp list

List discovered MCP servers

### Synopsis

Lists merged MCP servers from Claude user settings, Claude user .mcp.json, and project .mcp.json.

Effects:
  Read-only. Reads MCP configuration files.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak mcp list [flags]
```

### Examples

```
  ak mcp list
  ak mcp list --json
```

### Options

```
  -h, --help             help for list
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### Options inherited from parent commands

```
      --claude-home string   Claude home override; defaults to AGENTKIT_CLAUDE_HOME or ~/.claude
      --cwd string           Project directory override; defaults to current working directory
```

### SEE ALSO

* [ak mcp](./ak_mcp)	 - Inspect local MCP server configuration
