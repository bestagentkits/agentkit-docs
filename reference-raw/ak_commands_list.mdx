---
title: "ak commands list"
description: "List installed commands"
generated: true
---

## ak commands list

List installed commands

### Synopsis

Enumerate commands installed on this machine, optionally filtered by kit or tag.

Effects:
  Read-only. Scans local installed commands.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak commands list [flags]
```

### Examples

```
  ak commands list
  ak commands list --kit engineer
  ak commands list --json
```

### Options

```
  -h, --help             help for list
      --json             Emit machine-readable JSON (implies --no-interactive)
      --kit string       Filter by kit name
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
      --tag strings      Filter by tag (repeatable)
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak commands](./ak_commands)	 - Per-slash-command management commands
