---
title: "ak codex-agent-runtime register"
description: "Idempotently register ak-agent-runtime in ~/.codex/config.toml"
generated: true
---

## ak codex-agent-runtime register

Idempotently register ak-agent-runtime in ~/.codex/config.toml

### Synopsis

Idempotently register ak-agent-runtime in ~/.codex/config.toml so Codex discovers the runtime on startup.

Effects:
  Writes or updates [mcp_servers.ak-agent-runtime] in ~/.codex/config.toml.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak codex-agent-runtime register [flags]
```

### Examples

```
  ak codex-agent-runtime register
  ak codex-agent-runtime register --codex-home ~/.codex
  ak codex-agent-runtime register --json
```

### Options

```
      --codex-home string   Override $CODEX_HOME (defaults to ~/.codex)
  -h, --help                help for register
      --json                Emit machine-readable JSON (implies --no-interactive)
      --no-interactive      Disable interactive prompts (CI-safe)
  -q, --quiet               Suppress non-error output on stderr
  -V, --verbose             Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes                 Assume yes for all prompts
```

### SEE ALSO

* [ak codex-agent-runtime](./ak_codex-agent-runtime)	 - Codex agent MCP dispatch runtime
