---
title: "ak skills install"
description: "Install a skill"
generated: true
---

## ak skills install

Install a skill

### Synopsis

Resolve a skill by &lt;kit&gt;/&lt;skill&gt; and install it into the local environment.

Effects:
  Writes skill files to the local installation directory.

Exit status:
  0   success
  1   command failure
  2   invalid flags or arguments
  5   skill not found
  6   already installed (re-run with --force)

```
ak skills install <kit>/<skill> [flags]
```

### Examples

```
  ak skills install engineer/agent-browser
  ak skills install engineer/agent-browser --force
```

### Options

```
      --force            Overwrite existing install
  -h, --help             help for install
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak skills](./ak_skills)	 - Browse and install kit skills
