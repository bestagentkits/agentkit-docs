---
title: "ak backups list"
description: "List all rollback snapshots (newest first)"
generated: true
---

## ak backups list

List all rollback snapshots (newest first)

### Synopsis

Enumerate every rollback snapshot under ~/.agentkit/backups/, ordered newest first.

Effects:
  Read-only. Reads ~/.agentkit/backups/.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak backups list [flags]
```

### Examples

```
  ak backups list
  ak backups list --json
```

### Options

```
  -h, --help             help for list
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak backups](./ak_backups)	 - Manage rollback snapshots
