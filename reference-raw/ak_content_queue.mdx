---
title: "ak content queue"
description: "Manage the scheduled post queue"
generated: true
---

## ak content queue

Manage the scheduled post queue

### Synopsis

Inspect and control the persistent scheduled-post queue.

### Examples

```
  ak content queue list
  ak content queue cancel <id>
  ak content queue run-pending
```

### Options

```
  -h, --help             help for queue
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak content](./ak_content)	 - Publish content to channels
* [ak content queue cancel](./ak_content_queue_cancel)	 - Cancel (remove) a scheduled post by ID
* [ak content queue list](./ak_content_queue_list)	 - List scheduled posts
* [ak content queue run-pending](./ak_content_queue_run-pending)	 - Fire all scheduled posts whose time has arrived
