---
title: "ak codex-agent-runtime unregister"
description: "Idempotently remove ak-agent-runtime from ~/.codex/config.toml"
generated: true
---

## ak codex-agent-runtime unregister

Idempotently remove ak-agent-runtime from ~/.codex/config.toml

### Synopsis

Idempotently remove ak-agent-runtime from ~/.codex/config.toml. Exits 0 if already absent.

Effects:
  Removes [mcp_servers.ak-agent-runtime] from ~/.codex/config.toml.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak codex-agent-runtime unregister [flags]
```

### Examples

```
  ak codex-agent-runtime unregister
  ak codex-agent-runtime unregister --codex-home ~/.codex
  ak codex-agent-runtime unregister --json
```

### Options

```
      --codex-home string   Override $CODEX_HOME (defaults to ~/.codex)
  -h, --help                help for unregister
      --json                Emit machine-readable JSON (implies --no-interactive)
      --no-interactive      Disable interactive prompts (CI-safe)
  -q, --quiet               Suppress non-error output on stderr
  -V, --verbose             Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes                 Assume yes for all prompts
```

### SEE ALSO

* [ak codex-agent-runtime](./ak_codex-agent-runtime)	 - Codex agent MCP dispatch runtime
