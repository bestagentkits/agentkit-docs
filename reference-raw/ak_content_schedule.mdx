---
title: "ak content schedule"
description: "Manage the content scheduler"
generated: true
---

## ak content schedule

Manage the content scheduler

### Synopsis

Manage the content scheduler daemon and automated publishing.

### Examples

```
  ak content schedule daemon
  ak content schedule daemon --poll-interval 30
```

### Options

```
  -h, --help             help for schedule
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak content](./ak_content)	 - Publish content to channels
* [ak content schedule daemon](./ak_content_schedule_daemon)	 - Start the long-running scheduler daemon
