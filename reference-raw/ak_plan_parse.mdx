---
title: "ak plan parse"
description: "Parse and display a structured plan view"
generated: true
---

## ak plan parse

Parse and display a structured plan view

### Synopsis

Parse a plan directory and print a structured summary of all phases, task counts, and progress. Use --json for machine-readable output.

Effects:
  Read-only.

Exit status:
  0   success
  1   plan directory not found or invalid format
  2   invalid flags or arguments

```
ak plan parse <path> [flags]
```

### Examples

```
  ak plan parse ./plans/my-plan
  ak plan parse ./plans/my-plan --json
```

### Options

```
  -h, --help             help for parse
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak plan](./ak_plan)	 - Manage AgentKit plan directories
