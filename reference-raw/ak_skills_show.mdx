---
title: "ak skills show"
description: "Show skill details"
generated: true
---

## ak skills show

Show skill details

### Synopsis

Print metadata, version, description, and requirements for a single skill.

Effects:
  Read-only. Reads metadata for the specified skill.

Exit status:
  0   success
  1   command failure
  2   invalid flags or arguments
  5   skill not found

```
ak skills show <kit>/<skill> [flags]
```

### Examples

```
  ak skills show engineer/agent-browser
  ak skills show engineer/agent-browser --json
```

### Options

```
  -h, --help             help for show
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak skills](./ak_skills)	 - Browse and install kit skills
