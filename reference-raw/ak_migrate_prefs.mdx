---
title: "ak migrate prefs"
description: "Migrate legacy ClaudeKit preferences into AgentKit config.yaml"
generated: true
---

## ak migrate prefs

Migrate legacy ClaudeKit preferences into AgentKit config.yaml

### Synopsis

Read legacy ClaudeKit .ck.json preferences and merge values into user and project AgentKit config.yaml.

Effects:
  Read-only with --dry-run (default). Without --dry-run, merges settings into config.yaml after --yes.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak migrate prefs [flags]
```

### Examples

```
  ak migrate prefs
  ak migrate prefs --dry-run=false --yes
  ak migrate prefs --json
```

### Options

```
      --dry-run          Preview the preference migration without writing any files (default true)
      --from string      Source to migrate from (currently supports "ck" for ClaudeKit) (default "ck")
  -h, --help             help for prefs
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak migrate](./ak_migrate)	 - Migrate an existing ClaudeKit install to AgentKit
