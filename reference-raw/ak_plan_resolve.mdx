---
title: "ak plan resolve"
description: "Resolve the plan matching the current git/filesystem context"
generated: true
---

## ak plan resolve

Resolve the plan matching the current git/filesystem context

### Synopsis

What it does:
  Match the local plan store against the current context: explicit --issue, then current git branch, then current git worktree, then the working directory. Prints the best match plus every candidate that matched.

Who it's for:
  Both: quick 'what plan am I on' checks and scripted context lookups.

When to use it:
  Before running a phase update, to confirm which plan record applies here.

Examples:
  ak plan resolve
  ak plan resolve --issue 1065
  ak plan resolve --json

What changes on disk:
  Read-only.

Output modes:
  pretty   default on TTY (colors, ASCII markers)
  plain    auto when stdout piped or --no-interactive
  json     --json (single-object envelope, NDJSON-safe)

Exit codes:
  0  success
  1  runtime error
  2  invalid flags
  3  user-cancel (SIGINT, prompt-cancel)
  1  plan store I/O error

```
ak plan resolve [flags]
```

### Options

```
  -h, --help             help for resolve
      --issue int        Explicit GitHub issue number to resolve against (highest precedence)
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak plan](./ak_plan)	 - Manage AgentKit plan directories
