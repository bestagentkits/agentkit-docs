---
title: "ak licenses"
description: "Show licensed AgentKit kits"
generated: true
---

## ak licenses

Show licensed AgentKit kits

### Synopsis

List the kits the current account is licensed for.

Effects:
  Read-only. Reads ~/.agentkit/auth/session.json and queries registry.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak licenses [flags]
```

### Examples

```
  ak licenses
  ak licenses --json
```

### Options

```
  -h, --help                  help for licenses
      --json                  Emit machine-readable JSON (implies --no-interactive)
      --no-interactive        Disable interactive prompts (CI-safe)
  -q, --quiet                 Suppress non-error output on stderr
      --registry-url string   AgentKit registry base URL (default: $AGENTKIT_REGISTRY_URL, then dev/staging https://staging.agentkit.best or release https://agentkit.best)
  -V, --verbose               Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes                   Assume yes for all prompts
```

### SEE ALSO

* [ak](./ak)	 - AgentKit CLI
