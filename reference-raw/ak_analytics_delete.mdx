---
title: "ak analytics delete"
description: "Delete the local analytics index"
generated: true
---

## ak analytics delete

Delete the local analytics index

### Synopsis

What it does:
  Previews the local analytics delete operation and applies it only after explicit confirmation.

Who it's for:
  Both: maintainers at a terminal and scripted maintenance jobs.

When to use it:
  When the private local analytics lifecycle needs an explicit state transition.

Examples:
  ak analytics delete
  ak analytics delete --yes --json --no-interactive

What changes on disk:
  Preview is read-only. Applying may update the disposable local analytics index; delete retains only a disabled intent marker.

Output modes:
  pretty   default on TTY (colors, ASCII markers)
  plain    auto when stdout piped or --no-interactive
  json     --json (single-object envelope, NDJSON-safe)

Exit codes:
  0  success
  1  runtime error
  2  invalid flags
  3  user-cancel (SIGINT, prompt-cancel)

```
ak analytics delete [flags]
```

### Options

```
  -h, --help             help for delete
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak analytics](./ak_analytics)	 - Control the private local analytics index
