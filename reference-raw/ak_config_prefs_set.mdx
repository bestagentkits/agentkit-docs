---
title: "ak config prefs set"
description: "Set a hook, journal.auto, or worktree.root preference in config.yaml"
generated: true
---

## ak config prefs set

Set a hook, journal.auto, or worktree.root preference in config.yaml

### Synopsis

Write a per-hook toggle, journal.auto, or worktree.root preference into config.yaml.

Effects:
  Backs up then rewrites the selected scope's config.yaml file.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak config prefs set <key> <value> [flags]
```

### Examples

```
  ak config prefs set hooks.simplify-gate false
  ak config prefs set journal.auto false
  ak config prefs set worktree.root ../worktrees --scope project
```

### Options

```
  -h, --help             help for set
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
      --scope string     which config to write: user (the AgentKit home) or project (.agentkit/config.yaml here) (default "user")
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak config prefs](./ak_config_prefs)	 - Inspect resolved AgentKit preferences
