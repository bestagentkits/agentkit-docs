---
title: "ak content queue list"
description: "List scheduled posts"
generated: true
---

## ak content queue list

List scheduled posts

### Synopsis

List scheduled posts in the persistent queue.

Effects:
  Read-only. Reads ~/.agentkit/content/queue.json.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak content queue list [flags]
```

### Examples

```
  ak content queue list
  ak content queue list --json
```

### Options

```
  -h, --help             help for list
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak content queue](./ak_content_queue)	 - Manage the scheduled post queue
