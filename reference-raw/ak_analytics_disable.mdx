---
title: "ak analytics disable"
description: "Disable indexed reads without deleting the index"
generated: true
---

## ak analytics disable

Disable indexed reads without deleting the index

### Synopsis

Disables the private local SQLite analytics index while keeping the database intact. Interactive terminals prompt before applying; pass --yes to bypass prompts.

Effects:
  Sets disabled intent marker; serving reverts to live collector.

Exit status:
  0   success
  1   command failure
  2   invalid flags or arguments
  3   preview complete (re-run with --yes to apply)

```
ak analytics disable [flags]
```

### Examples

```
  ak analytics disable
  ak analytics disable --yes
```

### Options

```
  -h, --help             help for disable
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak analytics](./ak_analytics)	 - Control the private local analytics index
