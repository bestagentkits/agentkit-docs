---
title: "ak orchestrate plan-status"
description: "Inspect persisted plan state and invalidate stale evidence"
generated: true
---

## ak orchestrate plan-status

Inspect persisted plan state and invalidate stale evidence

### Synopsis

Inspect persisted plan state and invalidate stale evidence. CLI dispatch requires Darwin; internal dispatch belongs to the active harness.

Effects:
  Writes private jobs.yaml, state.json and metrics.jsonl in the run directory. Acceptance runs declared verification commands; advance may copy declared artifacts and spawn CLI jobs.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak orchestrate plan-status <run-dir> [flags]
```

### Examples

```
  ak orchestrate plan-status ./run
  ak orchestrate plan-status ./run --json
```

### Options

```
  -h, --help             help for plan-status
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak orchestrate](./ak_orchestrate)	 - Coordinate and inspect agent jobs (process supervision: Darwin)
