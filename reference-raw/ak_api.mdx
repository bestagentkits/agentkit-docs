---
title: "ak api"
description: "Run a local API + proxy server for AgentKit"
generated: true
---

## ak api

Run a local API + proxy server for AgentKit

### Synopsis

Run a local API and proxy server for AgentKit. Proxies LLM requests and exposes local health, status, and version endpoints.

### Examples

```
  ak api start
  ak api status
  ak api stop
```

### Options

```
  -h, --help             help for api
      --json             Emit machine-readable JSON (implies --no-interactive)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak](./ak)	 - AgentKit CLI
* [ak api start](./ak_api_start)	 - Start the local API + proxy server
* [ak api status](./ak_api_status)	 - Show the running state of the API server
* [ak api stop](./ak_api_stop)	 - Stop the running API server daemon
