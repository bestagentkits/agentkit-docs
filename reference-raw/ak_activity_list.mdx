---
title: "ak activity list"
description: "List recent local activity events"
generated: true
---

## ak activity list

List recent local activity events

### Synopsis

Returns a finite snapshot of the local activity log, newest first.

Effects:
  Read-only. Reads local activity events.

Exit status:
  0 success, 1 command failure, 2 invalid flags or arguments

```
ak activity list [flags]
```

### Examples

```
  ak activity list
  ak activity list --limit 20
  ak activity list --json
```

### Options

```
  -h, --help             help for list
      --json             Emit machine-readable JSON (implies --no-interactive)
      --limit int        Maximum events to return (default 100)
      --no-interactive   Disable interactive prompts (CI-safe)
  -q, --quiet            Suppress non-error output on stderr
      --since string     Return events with IDs greater than this cursor
  -V, --verbose          Extra diagnostic output on stderr (loses to --quiet)
  -y, --yes              Assume yes for all prompts
```

### SEE ALSO

* [ak activity](./ak_activity)	 - Inspect the local AgentKit activity feed
