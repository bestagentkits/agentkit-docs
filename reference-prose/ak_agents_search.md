Search available agents by name, description, and tags. Results are ranked deterministically; ties are broken by `<kit>/<agent>` alphabetical order.

**When to use it:** To find an agent by keyword when you don't know the exact `<kit>/<agent>` ref. Follow up with `ak agents show` before installing.

This command is read-only.
