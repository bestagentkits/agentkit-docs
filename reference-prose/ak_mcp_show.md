Show one discovered MCP server, including args and environment key names. Environment values are never displayed.

**When to use it:** Before editing or linking MCP config for a specific server.

Read-only.
