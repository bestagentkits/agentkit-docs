List local versions for `ak`, kits, and skills to audit what is installed or diagnose version mismatches.

**When to use it:** When you need a local inventory of installed component versions.

Read-only unless a versions registry endpoint is configured and returns a fresh cacheable response. Live latest-version comparison is disabled until the AgentKit versions registry endpoint is deployed.
