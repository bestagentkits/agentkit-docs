Show skill workflow graph relationships from metadata, Workflow Position sections, and optional `/ak:` co-mention heuristics with `--heuristic`.

**When to use it:** Before editing skill workflow metadata, while auditing skill discovery, or when checking why one skill should typically precede or follow another.

Read-only. Loads local kit metadata and skill markdown through KitSource.
