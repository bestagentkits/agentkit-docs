Mirror MCP servers from one adapter config into another. Dry-run by default; pass `--apply` to write the target adapter config after taking a backup.

**When to use it:** After `ak mcp list` to preview or apply cross-adapter MCP config. `--to codex` is currently supported.

Dry-run by default unless `--apply` is set.
