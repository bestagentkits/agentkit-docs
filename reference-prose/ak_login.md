Log in to AgentKit so you can install or update licensed remote kits. Supports license key device activation, email OTP, and user API key methods.

**When to use it:** Before installing or updating licensed remote kits. Raw credentials are not persisted.

Writes the session to `~/.agentkit/auth/session.json`.
